apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: weave
  namespace: weave
spec:
  rules:
  - host: scope.localhost
    http:
      paths:
      - path: /
        backend:
          serviceName: weave-scope-app
          servicePort: 80
