apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: nodejs-example
  name: nodejs-example
spec:
  replicas: 1
  # selector:
  #   matchLabels:
  #     app: nodejs-example
  # strategy: {}
  template:
    # metadata:
    #   creationTimestamp: null
    #   labels:
    #     app: nodejs-example
    spec:
      containers:
      - image: docker.for.mac.host.internal:5000/debian:latest
        name: nodejs-example
        ports:
        - containerPort: 3000
